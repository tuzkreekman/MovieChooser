To Do:
- Choose top of suggested based on tropes/ratings
- Check if needed: encode title in calls to other functions (for url)

Possibly:
- Amazon
- RottenTomatoes (seems simple enough)

Wrapup:
- Copy test makefile to /movie_chooser
- Remove /how_to_compile.txt
- Create SQL for schema